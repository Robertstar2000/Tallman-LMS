import { User, UserRole, Course, CourseStatus, Enrollment, Badge, Module, Branch, Category, ForumPost } from './types';

// Enterprise Data Registry
// All course tracks, technical manuals, and user metadata have been migrated to the SQLite database.
// This file can be used for runtime-only types or temporary mock state if required during development.